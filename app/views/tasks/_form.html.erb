<% if @task.errors.present? %>
    <p>
        <%= @task.errors.full_messages.join(', ') %>
    </p>
<% end %>

<%= form_with model: @task, local: true do |f| %>
    <div>
        <%= f.label :title %>
        <%= f.text_field :title %>
        <%= f.submit %>
    </div>
<% end %>